<%= form_for(current_user.joining_relationships.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :joined_project_id, @project.id %></div>
  <%= f.submit "プロジェクト参加メンバーになる。", class: "btn btn-primary" %>
<% end %>